# Dependências do Node
node_modules/

# Arquivos de log do npm
npm-debug.log*

# Ignora artefatos de build (pacotes gerados)
*.zip

# Arquivo regerado pelo build.sh, não precisa versionar
src/version.js
